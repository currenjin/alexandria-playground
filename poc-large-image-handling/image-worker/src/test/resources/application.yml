spring:
  application:
    name: image-worker-test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

image:
  processing:
    max-file-size-bytes: 10485760
    max-pixels: 25000000

logging:
  level:
    com.example: DEBUG
    org.testcontainers: INFO
