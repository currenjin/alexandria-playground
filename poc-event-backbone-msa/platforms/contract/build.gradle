// 공유 계약 모듈 (§7.1.7): 이벤트·커맨드 record + @EventContract. Spring 의존 없음.
// 모든 서비스가 이 모듈에만 의존, 서비스끼리는 서로 의존하지 않는다.
// java-library 플러그인은 root 라이브러리 그룹에서 적용된다.
dependencies {
    // 예제 계약은 계약 메커니즘(@EventContract·DomainEvent)만 라이브러리에서 가져다 쓴다.
    api project(':platforms:core')
}
